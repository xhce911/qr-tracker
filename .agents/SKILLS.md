# SKILLS — QR Tracker Agent Capabilities

## Frontend
- [x] Generar componentes React funcionales con hooks (`useState`, `useEffect`, `useNavigate`, etc.)
- [x] Implementar formularios controlados con validación cliente.
- [x] Crear modales, tabs, dropdowns y layouts responsive con HeroUI.
- [x] Manejar estados de carga (`Spinner`, skeletons) y errores (alerts, toasts).
- [x] Implementar routing declarativo con `react-router-dom` (rutas públicas y protegidas).
- [x] Aplicar estilos con Tailwind CSS y variables CSS custom.
- [x] Consumir y renderizar códigos QR (`qrcode.react`).
- [x] Manejar errores de chunk loading de webpack (CRA).

## Backend / Firebase
- [x] Configurar e inicializar Firebase App (Auth, Firestore, Functions, Analytics).
- [x] Implementar autenticación con email/password y proveedores OAuth (Google).
- [x] Escuchar estado de autenticación con `onAuthStateChanged`.
- [x] Crear, leer, actualizar y eliminar documentos en Firestore.
- [x] Escribar Cloud Functions (aunque el proyecto aún no las tenga activas).
- [x] Manejar errores de Firebase y traducirlos al español.

## Debugging & DevOps
- [x] Leer logs del navegador y de Firebase para diagnosticar errores.
- [x] Verificar builds limpios con `npm run build`.
- [x] Limpiar caché de webpack y reinstalar dependencias.
- [x] Configurar Firebase Hosting (`firebase.json`, `.firebaserc`).

## Limitaciones (NO hacer)
- [ ] No tienes acceso a la consola de Firebase (producción).
- [ ] No puedes crear cuentas de Google o verificar dominios OAuth.
- [ ] No ejecutas operaciones de git mutantes sin confirmación.
- [ ] No modificas la configuración de webpack de CRA (a menos que sea con `react-app-rewired` y el usuario lo apruebe).
- [ ] No migras el proyecto a otro framework sin discusión previa.
